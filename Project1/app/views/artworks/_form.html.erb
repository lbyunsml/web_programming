<%= form_with(model: artwork, local: true) do |form| %>
  <% if artwork.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(artwork.errors.count, "error") %> prohibited this artwork from being saved:</h2>

      <ul>
      <% artwork.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :artwork_title %>
  </div>

  <div class="field">
    <%= form.label :img_path %>
    <%= form.text_field :img_path, id: :artwork_img_path %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :artwork_description %>
  </div>

  <div class="field">
    <%= form.label :category %>
    <%= form.text_field :category, id: :artwork_category %>
  </div>

  <div class="field">
    <%= form.label :artist_id %>
    <%= form.text_field :artist_id, id: :artwork_artist_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
