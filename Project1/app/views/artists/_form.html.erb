<%= form_with(model: artist, local: true) do |form| %>
  <% if artist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(artist.errors.count, "error") %> prohibited this artist from being saved:</h2>

      <ul>
      <% artist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :login_id %>
    <%= form.text_field :login_id, id: :artist_login_id %>
  </div>

  <div class="field">
    <%= form.label :password %>
    <%= form.text_field :password, id: :artist_password %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :artist_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :artist_description %>
  </div>

  <div class="field">
    <%= form.label :profile_img %>
    <%= form.text_field :profile_img, id: :artist_profile_img %>
  </div>

  <div class="field">
    <%= form.label :mail %>
    <%= form.text_field :mail, id: :artist_mail %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
